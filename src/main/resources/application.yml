spring:
  application:
    name: wiremock-handler
server:
  port: 8088
management:
  endpoints:
    web:
      exposure:
        include: health,beans

net:
  snytkine:
    rest-client-middleware:
      wiremock:
        enabled: true
        mappings-class-path: mappings
        proxy-pass-through: true
